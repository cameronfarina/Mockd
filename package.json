{
  "name": "mockd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "tsx src/cli.ts validate",
    "keepers": "tsx src/cli.ts keepers",
    "profiles": "tsx src/cli.ts profiles",
    "mock": "tsx src/cli.ts mock"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
